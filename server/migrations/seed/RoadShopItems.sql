-- Hunting Road has a native catalog in the ZZ client.
-- ShopType 10 rows are optional additions/overrides, not that native catalog.
-- Leave the default empty to retain original item prices and unlock rules.
-- Existing installations: see ../manual/restore_native_road_shop.sql.
SELECT 1;
